<%= attachment_image_tag(comment.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", style: "display: block") %>
<%= comment.comment %> by <%= comment.user.name %>
	<% if current_user.id == comment.user_id %>
		<%= link_to '削除する', book_comment_path(comment.id), method: :delete, class:"btn btn-danger", remote: true %>
	<% end %>